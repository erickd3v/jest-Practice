const value = (x) => {
    if(typeof x === 'undefined') return
    for (let i = 0; i < x.length; i++) {
        const element = x.length;
        return element
    }
}

module.exports = {
    value
}